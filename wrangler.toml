# Cloudflare Pages project: tohnee-ai
# Compat date for Workers features (not used by Pages, but required field)
compatibility_date = "2026-05-01"

# Build output dir is passed via CLI: `wrangler pages deploy dist`
# Custom domain is configured in Cloudflare Dashboard: tohnee.ai
